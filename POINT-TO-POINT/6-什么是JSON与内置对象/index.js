//Json 是一个内置对象,也是一种数据格式

// Json只有两个API

//转字符串
JSON.stringify({ a: 10, b: 20 });

//转JSON
JSON.parse('{"a":10,"b":20}');

//Math，也是内置对象

var arr = new Array(1, 2, 3);

console.log(arr);
